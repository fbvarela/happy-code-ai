### configuration feature

### Goal
- The user might be able to see and modify the repo ai config files the same way is done for the memory docs: https://code-dev.happyfactory.app/memory.
- Add a configuration guide similar to the memory guide.